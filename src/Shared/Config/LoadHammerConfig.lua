--!strict

export type Config = {}

local DEFAULT_CONFIG: Config = {}

local function LoadHammerConfig(configuration: Configuration): Config
	local config = DEFAULT_CONFIG

	return config
end

return LoadHammerConfig
